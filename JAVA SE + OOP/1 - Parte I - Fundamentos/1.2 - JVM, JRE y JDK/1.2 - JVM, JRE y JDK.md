#java #jvm #jre #jdk #programacion #conceptos

---
# JVM, JRE y JDK

## 1) Descripción

En esta unidad se estudian los tres componentes fundamentales del ecosistema Java: **JVM**, **JRE** y **JDK**.  
Estos elementos explican **cómo se ejecuta Java**, por qué es **multiplataforma** y qué herramientas necesita un desarrollador para **programar, compilar y ejecutar** aplicaciones.

Este tema es clave porque marca la diferencia entre:
- ✅ usar Java
- ✅ desarrollar en Java
- ✅ entender cómo funciona internamente

---

## 2) Teoría

### 🔹 JVM – Java Virtual Machine

La **JVM** es la máquina virtual encargada de **ejecutar el bytecode Java**.

**Funciones principales:**
- Ejecuta archivos `.class`
- Gestiona la memoria (Heap y Stack)
- Maneja hilos de ejecución
- Ejecuta el Garbage Collector
- Aísla el programa del sistema operativo

> El código Java **no se ejecuta directamente en el sistema operativo**, sino dentro de la JVM.

**Flujo de ejecución:**
```java
Archivo .java  
↓ (javac)  
Archivo .class (bytecode)  
↓  
JVM  
↓  
Sistema Operativo
```


Esto permite que Java sea **independiente del sistema operativo**.

---

### 🔹 JRE – Java Runtime Environment

La **JRE** es el entorno necesario para **ejecutar aplicaciones Java**.

Incluye:
- JVM
- Bibliotecas estándar (Java API)
- Archivos de soporte

> ❌ No incluye herramientas de desarrollo como el compilador

Se utiliza cuando:
- Solo se necesita **ejecutar** un programa Java
- No se va a **escribir ni compilar** código

---

### 🔹 JDK – Java Development Kit

El **JDK** es el kit completo para **desarrollar en Java**.

Incluye:
- JRE
- JVM
- Compilador `javac`
- Herramientas como:
  - `java`
  - `javadoc`
  - `jar`
  - `jshell`

> Todo desarrollador Java necesita un **JDK instalado**.

---

### 🔹 Relación entre JVM, JRE y JDK

```java
JDK  
└── JRE  
└── JVM
```


- **JVM** → ejecuta
- **JRE** → ejecuta + librerías
- **JDK** → desarrolla + compila + ejecuta

---

## 3) Integración RPG

En un juego RPG desarrollado en Java:

- La **JVM** gestiona la memoria de personajes, enemigos y mapas
- El **Garbage Collector** elimina objetos no utilizados (enemigos derrotados)
- El **JDK** permite crear y compilar nuevas mecánicas
- La **JRE** es suficiente para que el jugador ejecute el juego

> El mismo RPG puede correr en **Windows, Linux o macOS** sin modificaciones.

---

## 4) Recursos

- Documentación oficial Java: [https://docs.oracle.com/javase/](https://docs.oracle.com/javase/)  
- Java Platform Overview: [https://www.oracle.com/java/technologies/](https://www.oracle.com/java/technologies/)

---

## 5) Notas adicionales

- ❌ Error común: instalar solo JRE y querer programar
- ✅ Buena práctica: usar versiones **LTS** del JDK
- 💡 La JVM no es exclusiva de Java (Kotlin, Scala también la utilizan)
- 📌 Cada versión de Java incluye su propia implementación de JVM
