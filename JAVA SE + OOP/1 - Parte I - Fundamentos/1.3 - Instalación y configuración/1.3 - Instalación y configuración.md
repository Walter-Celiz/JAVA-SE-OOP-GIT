
#java #jdk #instalacion #configuracion #entorno #java-se

---

## 1) Descripción

En esta unidad aprenderás **cómo instalar y configurar correctamente Java (JDK)** en tu sistema, dejando el entorno listo para desarrollar aplicaciones Java desde la consola, un editor o un IDE.

Esta etapa es **fundamental**, ya que:
- Sin una instalación correcta, **no podrás compilar ni ejecutar código Java**
- Una mala configuración del entorno suele ser una de las **principales causas de errores iniciales**
- Es la base sobre la cual se apoyan **todas las unidades siguientes**

---

## 2) Teoría

### ¿Qué necesitamos para programar en Java?

Para desarrollar en Java es necesario:

- **JDK (Java Development Kit)**  
  Incluye:
  - Compilador (`javac`)
  - Máquina virtual (JVM)
  - Librerías estándar
  - Herramientas de desarrollo

⚠️ **No alcanza solo con la JVM o el JRE**, para programar siempre necesitamos el **JDK**.

---

### Versiones de Java

Java tiene distintas versiones:
- **LTS (Long Term Support)** → recomendadas para aprender y trabajar  
  Ejemplos: Java 8, 11, 17, 21
- **No LTS** → versiones intermedias con soporte corto

👉 Para este proyecto se recomienda **Java 21 o superior (LTS)**.

---

### Instalación del JDK (conceptual)

El proceso general es el mismo en todos los sistemas:

1. Descargar el JDK desde una fuente oficial
2. Instalarlo en el sistema
3. Configurar variables de entorno
4. Verificar la instalación desde la consola

---

### Variables de entorno

Las variables de entorno permiten que el sistema sepa **dónde está instalado Java**.

Las más importantes son:

#### `JAVA_HOME`
- Apunta a la carpeta donde está instalado el JDK
- Es utilizada por herramientas, IDEs y frameworks

Ejemplo: `JAVA_HOME=C:\Program Files\Java\jdk-21`

#### `PATH`
- Permite ejecutar comandos como `java` y `javac` desde cualquier carpeta

Se agrega: `%JAVA_HOME%\bin`

---

### Verificación de la instalación

Desde la terminal o consola:
```java
java -version  
javac -version
```

Si ambos comandos funcionan, el entorno está correctamente configurado ✅

---

## 3) Código

> En esta unidad **no escribimos código Java aún**, pero verificamos que el compilador funcione correctamente.

Ejemplo de verificación: 
```java
javac --help
```

Si el comando responde mostrando opciones, el JDK está operativo.


---

## 4) Integración RPG

En el proyecto RPG:

- Java será el **motor del juego**
- El JDK permitirá:
- Compilar el código del juego
- Ejecutar combates, lógica y estados
- Una mala configuración impediría incluso **iniciar el juego**

🧙‍♂️ *Instalar Java es como preparar el mundo antes de que aparezcan los personajes.*

---

## 5) Recursos

- Documentación oficial de Java
- OpenJDK
- Guías de instalación según sistema operativo
- IDEs recomendados:
- IntelliJ IDEA
- Eclipse
- VS Code + Extension Pack for Java

📁 Recursos adicionales se almacenan en la carpeta `resources/`.

---

## 6) Notas adicionales

- Siempre abre **una nueva terminal** después de modificar variables de entorno
- No mezclar múltiples versiones de Java sin saber cuál está activa
- Evitar rutas con espacios mal configurados
- Si `java` funciona pero `javac` no → probablemente esté instalado solo el JRE

